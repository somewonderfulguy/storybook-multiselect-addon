import { styled } from '@storybook/theming'

const StyledResetWrapper = styled.div`
  display: flex;
  align-items: center;
  gap: 4px;
`
StyledResetWrapper.displayName = 'StyledResetWrapper'

export { StyledResetWrapper }