import { styled } from '@storybook/theming'

const StyledErrorIconContainer = styled.div`
  & > svg {
    margin: 13px 7px 8px 11px;
    cursor: not-allowed;
  }
`

export { StyledErrorIconContainer }
